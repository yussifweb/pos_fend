import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.amspos.app',
  appName: 'AMS POS',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
