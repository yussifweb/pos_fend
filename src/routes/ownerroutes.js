import AddCategory from "../components/category/AddCategory";

const ownerroutes = [
    {path: '/owner', exact: true, name: 'Owner'},
    // {path: "/add-category", exact: true, component: AddCategory}
    // {path: '/owner/dashboard', exact: true, name: 'Dashboard', component: Dashboard},
];

export default ownerroutes;